node_modules/
.env
uploads/
sessions/
orders/
backups/
.claude/
*.db
*.log
.DS_Store
Thumbs.db
.playwright-mcp/
# Stray screenshots/artifacts captured at repo root (e.g. from Playwright sessions).
# Scoped to root so tracked assets like public/og-image.png are unaffected.
/*.png
.codex/
